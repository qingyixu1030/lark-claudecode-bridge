[project]
name = "lark-claudecode-bridge"
version = "0.1.0"
description = "Bridge Claude Code CLI with Feishu/Lark via WebSocket"
requires-python = ">=3.11"
license = "MIT"
authors = [
    { name = "Cindy Xu" },
]
dependencies = [
    "lark-oapi>=1.3.0",
    "python-dotenv>=1.0.0",
]
